name: Deploy openscientist (prod)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

concurrency:
  # Serialize deploys per env: az vm run-command allows only one execution per VM
  # at a time, so overlapping runs (e.g. batch-merges to main) hit a Conflict and
  # fail. Queue them instead of racing; never cancel an in-flight deploy midway.
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Build base + main image in ACR
        run: |
          ACR=${{ secrets.ACR_NAME }}
          sed -i "s|^FROM openscientist-base:latest|FROM ${ACR}.azurecr.io/openscientist-base:latest|" Dockerfile Dockerfile.agent Dockerfile.executor
          az acr build -r $ACR -t openscientist-base:latest -f Dockerfile.base .
          # The agent and executor images run the same src/ as the web image.
          # They used to be built by hand, so they silently froze while the web
          # image kept shipping: the agent stayed on a June build that predates
          # the key-replacement proxy, so it took the placeholder key the runner
          # hands it and sent that straight to Azure, failing every job with 401.
          # Dockerfile.agent compiles codex from source, which runs well past
          # ACR's default 1h step timeout (a build without this flag was killed
          # mid-compile at exactly 1h0m0s). Raised so the deploy fails on real
          # errors rather than the clock.
          az acr build -r $ACR --timeout 21600 -t openscientist-agent:latest -f Dockerfile.agent .
          az acr build -r $ACR -t openscientist-executor:latest -f Dockerfile.executor .
          TAG=prod-${{ github.sha }}
          az acr build -r $ACR -t openscientist:$TAG -f Dockerfile .
          echo "TAG=$TAG" >> "$GITHUB_ENV"

      - name: Redeploy on vm-openscientist-prod
        run: |
          ACR=${{ secrets.ACR_NAME }}
          FULL=${ACR}.azurecr.io/openscientist:${{ env.TAG }}
          TOK=$(az acr login -n $ACR --expose-token --query accessToken -o tsv)
          # The image ships migrations but nothing applied them, so a release
          # carrying a schema change left the database behind and every query
          # touching a new column failed at runtime. Migrating between the pull
          # and the restart means the new code never starts against an old
          # schema, and a failed migration aborts the deploy with the previous
          # container still serving.
          az vm run-command invoke -g rg-openscientist-prod -n vm-openscientist-prod --command-id RunShellScript --scripts "docker login ${ACR}.azurecr.io -u 00000000-0000-0000-0000-000000000000 -p '$TOK' && sed -i 's|image: acrcbraindev.azurecr.io/openscientist:.*|image: $FULL|' /opt/openscientist/docker-compose.yml && cd /opt/openscientist && docker compose pull openscientist && docker compose run --rm openscientist alembic upgrade head && docker compose up -d openscientist"
